# 樂活五線譜（自動抓股價版）— Google Sheets 設定教學

只能在 **Google Sheets** 使用（`GOOGLEFINANCE` 是 Google 專屬雲端函數，Excel 沒有這個功能）。
開一個空白 Google Sheets，照下面順序貼公式即可。

---

## 第一步：輸入區（B1、B2）

| 儲存格 | 內容 |
|---|---|
| A1 | `股票代號` |
| B1 | `2330` ← 換成你要查的股票代號 |
| A2 | `回測年數` |
| B2 | `3` |
| A3 | `起始日` |
| B3 | `=TODAY()-B2*365` |
| A4 | `結束日` |
| B4 | `=TODAY()` |

**代號輸入規則：**
- 台股：直接輸入數字代號，例如 `2330`、`0050`（公式會自動加上 `TPE:` 前綴）
- 美股：直接輸入代號，例如 `AAPL`、`VOO`
- 若台股抓不到資料，改試 `TPE:2330`（上市）或 `TPEX:代號`（上櫃）

---

## 第二步：抓歷史股價（A6 開始）

在 **A6** 貼上：

```
=GOOGLEFINANCE(IF(REGEXMATCH(B1,"^[0-9]+$"),"TPE:"&B1,B1),"close",B3,B4)
```

這會自動「溢出」成兩欄：A 欄是日期、B 欄是收盤價，往下一路填到資料結束為止，
不需要手動下拉。

> 這時 A6:B6 是標題列（Date / Close），資料從第 7 列開始。

---

## 第三步：計算天數 X（C 欄）

**C7** 貼上：

```
=ARRAYFORMULA(IF(A7:A3000="","",ROW(A7:A3000)-ROW(A7)+1))
```

會自動幫每一列資料編號 1、2、3…，並在資料結束的地方自動停止（空白列不會有數字）。

---

## 第四步：斜率、截距、標準差（K1、K2、L1）

| 儲存格 | 公式 | 說明 |
|---|---|---|
| J1 | `斜率 Slope` | 標籤 |
| K1 | `=SLOPE(FILTER(B7:B3000,A7:A3000<>""),FILTER(C7:C3000,A7:A3000<>""))` | 迴歸線斜率 |
| J2 | `截距 Intercept` | 標籤 |
| K2 | `=INTERCEPT(FILTER(B7:B3000,A7:A3000<>""),FILTER(C7:C3000,A7:A3000<>""))` | 迴歸線截距 |

---

## 第五步：迴歸值、偏差值（D、E 欄）

**D7** 貼上：
```
=ARRAYFORMULA(IF(A7:A3000="","",$K$1*C7:C3000+$K$2))
```

**E7** 貼上：
```
=ARRAYFORMULA(IF(A7:A3000="","",B7:B3000-D7:D3000))
```

---

## 第六步：標準差（L1）

| 儲存格 | 公式 |
|---|---|
| J3 | `標準差` |
| L1 | `=STDEV(FILTER(E7:E3000,A7:A3000<>""))` |

---

## 第七步：五線譜的四條外側線（F、G、H、I 欄）

| 欄 | 標題 | 公式（填在第 7 列，會自動整欄套用） |
|---|---|---|
| F7 | 樂觀線 +2SD | `=ARRAYFORMULA(IF(A7:A3000="","",D7:D3000+2*$L$1))` |
| G7 | 相對樂觀 +1SD | `=ARRAYFORMULA(IF(A7:A3000="","",D7:D3000+1*$L$1))` |
| H7 | 相對悲觀 −1SD | `=ARRAYFORMULA(IF(A7:A3000="","",D7:D3000-1*$L$1))` |
| I7 | 悲觀線 −2SD | `=ARRAYFORMULA(IF(A7:A3000="","",D7:D3000-2*$L$1))` |

---

## 第八步：目前位置判讀（選用，K5:K7）

| 儲存格 | 公式 |
|---|---|
| K5 | `=B7` （最新一筆收盤價，可改成 `=INDEX(FILTER(B7:B3000,A7:A3000<>""),COUNTA(FILTER(B7:B3000,A7:A3000<>"")))` 取最後一筆） |
| K6 | `=INDEX(FILTER(E7:E3000,A7:A3000<>""),COUNTA(FILTER(E7:E3000,A7:A3000<>"")))` （最新偏差值） |
| K7 | `=K6/L1` （目前是幾個標準差） |
| K8 | `=IF(K7>=2,"接近樂觀線，評價偏貴",IF(K7<=-2,"接近悲觀線，評價偏便宜",IF(K7>=1,"相對樂觀區間",IF(K7<=-1,"相對悲觀區間","趨勢線附近，中性"))))` |

---

## 第九步：畫圖

1. 選取 **A6:B3000**，按住 Ctrl 再加選 **D6:D3000** 和 **F6:I3000**（欄位不連續用 Ctrl 多選）
2. 插入 → 圖表
3. 圖表類型選「折線圖」
4. 圖表右上角「自訂」→「數列」→「遺漏的資料點」選 **「不要繪製」**，這樣空白列才不會被畫成 0
5. 完成！之後只要改 **B1 的股票代號**，全部資料、公式、圖表都會自動更新

---

## 常見問題

- **`#N/A` 或抓不到資料**：代號打錯，或該股票 Google Finance 沒有收錄（部分冷門櫃買股票常發生），換代號格式再試一次
- **資料只到某天就停了**：GOOGLEFINANCE 對當日盤中資料有延遲，正常現象
- **公式跑很慢**：把 `3000` 改小一點（例如抓 3 年約 750 個交易日，改成 `1000` 就夠用了，能加快試算表運算速度）
